<templates>
  <!-- <t t-name="o-spreadsheet-Dashboard" owl="1">
    <div class="o-spreadsheet" t-on-keydown="onKeydown">
      <div
        class="o-grid"
        t-on-wheel="onMouseWheel"
        t-ref="grid">
        <div
          t-ref="gridOverlay"
          tabindex="-1"
          class="o-grid-overlay"
          t-att-style="gridOverlayStyle"
          t-on-mousedown="onMouseDown">
          <FiguresContainer ="props.model"/>
        </div>
        <canvas t-ref="canvas"/>
        <t
          t-set="gridSize"
          t-value="env.model.getters.getMaxViewportSize(env.model.getters.getActiveSheet())"
        />
        <div
          class="o-scrollbar vertical"
          t-on-scroll="onScroll"
          t-ref="vscrollbar"
          t-att-style="vScrollbarStyle">
          <div t-attf-style="width:1px;height:{{gridSize.height}}px"/>
        </div>
        <div
          class="o-scrollbar horizontal"
          t-on-scroll="onScroll"
          t-ref="hscrollbar"
          t-att-style="hScrollbarStyle">
          <div t-attf-style="height:1px;width:{{gridSize.width}}px"/>
        </div>
        <div class="o-scrollbar corner"/>
      </div>
    </div>
  </t> -->
</templates>
